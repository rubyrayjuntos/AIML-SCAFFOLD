# R3.2 — Platform Foundation implementation plan

Status: **approved for execution 2026-08-18; revised same day to group Steps B–G (Databricks through evidence, including GitHub Actions) as one deliverable rather than treating CI as a later add-on.** Step A is closed and `infra/platform_foundation/` is initialized. Each subsequent lettered step remains its own separate plan/review/apply/verify boundary (see Implementation plan's execution-rhythm note) — grouping B–G as one *deliverable* does not mean one bundled *authorization*.

Scope per ADR 0012: provision/adopt Databricks and Foundry on the primary subscription through the new `infra/platform_foundation/` Terraform root. **Not in scope**: Unity Catalog, vector indexes, churn workload migration (R3.4), functional auth proof (R3.3), OpenTelemetry instrumentation (R5), the `azmlops`/backend-storage-account questions R3.1 explicitly deferred.

## Explore findings

### A. Databricks target

- Resource type: `azurerm_databricks_workspace` (confirmed present in the installed azurerm ~>4.0 provider via `terraform providers schema -json`, not assumed from docs).
- Required fields: `name`, `resource_group_name`, `location`, `sku`. `sku` must be `"premium"` — Unity Catalog (R3.4) requires Premium tier, and upgrading tiers later is disruptive; choosing Premium now avoids a forced migration.
- `managed_resource_group_name` (optional) should be set explicitly for naming consistency, matching this repo's existing naming convention. Azure auto-creates this managed resource group and its contents (a VNet, an NSG, a DBFS-root storage account, and Databricks' own system resources) the moment the workspace is created — **Terraform does not and should not declare those internals**, only the `azurerm_databricks_workspace` resource itself. This is the concrete answer to "identities Azure creates automatically vs. identities Terraform must own."
- Unity Catalog prerequisites: **none belong in R3.2.** UC metastore assignment happens at the Databricks *account* level via the separate `databricks` Terraform provider (account-admin authenticated, distinct from `azurerm`), not the workspace-level `azurerm` resource. R3.2 provisions a Premium workspace capable of UC later; R3.4 does the actual metastore/catalog work.
- Region: confirmed live via `az provider show --namespace Microsoft.Databricks` — `eastus` is available for `Microsoft.Databricks/workspaces` on this subscription.
- Cost: confirmed via research before Step B's apply — Azure Databricks does not charge DBUs for idle infrastructure; a bare workspace (any SKU tier, no clusters/serverless endpoints running) has no meaningful standing cost. Premium vs. Standard affects per-DBU rates and feature gating (Unity Catalog access) when compute *is* running, not idle cost. The only components with a baseline always-on charge are serverless SQL warehouses and model-serving endpoints, neither of which R3.2 provisions. Sources: [Azure Databricks Pricing](https://azure.microsoft.com/en-us/pricing/details/databricks/), [Scale to zero - Azure Databricks](https://learn.microsoft.com/en-us/azure/databricks/oltp/projects/scale-to-zero).

### B. Foundry adoption

Live-verified facts about the existing resource (`rg-RSwan-1970`, `2026-08-18`):

| Fact | Value |
|---|---|
| Account resource ID | `/subscriptions/5b452321-32fd-4b1c-8bbf-6d69a5a587ad/resourceGroups/rg-RSwan-1970/providers/Microsoft.CognitiveServices/accounts/rswan-1970-resource` |
| kind | `AIServices` |
| **location** | **`eastus2`** — not `eastus`, where the rest of the primary subscription's infra lives. R3.2 imports as-is rather than relocating (Azure resources generally can't be moved regions in place); this is a real, permanent inconsistency to accept, not silently normalize away. |
| sku | `S0` |
| `customSubDomainName` | `rswan-1970-resource` |
| `publicNetworkAccess` | `Enabled` |
| `disableLocalAuth` | `False` (API-key auth is currently allowed, not AAD-only — worth a deliberate decision at R3.5, not silently changed here) |
| `networkAcls` | default-allow, no restrictions |
| identity | `SystemAssigned`, principalId `6247489d-b1d9-43fd-8a2b-fbb2587f80cf` |
| tags | none set |
| Associated project | `rswan-1970` (child `Microsoft.CognitiveServices/accounts/projects` resource) |
| Deployment: `gpt-5` | sku `GlobalStandard`, capacity `50`, model version `2025-08-07`, RAI policy `Microsoft.DefaultV2` |
| Deployment: `text-embedding-3-small` | sku `Standard`, capacity `120`, model version `1`, RAI policy `Microsoft.DefaultV2` |

Terraform coverage (confirmed present in the installed provider schema, not assumed):

| Live resource | Terraform resource type | Required fields (from installed schema) |
|---|---|---|
| The `AIServices` account | `azurerm_cognitive_account` (`kind = "AIServices"`) — **corrected during Step C's declaration**: `azurerm_ai_services`, this table's original recommendation, is flagged deprecated by the installed provider itself (`terraform validate` warning: "replaced by the azurerm_cognitive_account resource... feature-frozen"), so `azurerm_cognitive_account` is used instead. Field names differ slightly from the deprecated resource: `local_auth_enabled` not `local_authentication_enabled`, `public_network_access_enabled` (bool) not `public_network_access` (string), plus a required `kind` field and a `project_management_enabled` field mapping to the live `allowProjectManagement: true` property this table didn't originally list. | `name`, `resource_group_name`, `location`, `sku_name`, `kind`; `custom_subdomain_name`, `public_network_access_enabled`, `local_auth_enabled`, `project_management_enabled` all map cleanly to the live values above |
| The child `rswan-1970` project | `azurerm_cognitive_account_project` | `cognitive_account_id`, `name`, `location` |
| `gpt-5` deployment | `azurerm_cognitive_deployment` | `cognitive_account_id`, `name`, `model {}` block, `sku {}` block |
| `text-embedding-3-small` deployment | `azurerm_cognitive_deployment` | same shape |

No `azapi_resource` escape hatch is needed — the installed azurerm provider has first-class coverage for every piece of this resource, including the newer AIServices/project shape.

Expected import addresses (standard azurerm convention — every resource type checked uses the full ARM resource ID as the import identifier):
- `azurerm_cognitive_account.foundry` ← the account resource ID above
- `azurerm_cognitive_account_project.reference` ← `.../accounts/rswan-1970-resource/projects/rswan-1970`
- `azurerm_cognitive_deployment.gpt5` ← `.../accounts/rswan-1970-resource/deployments/gpt-5`
- `azurerm_cognitive_deployment.embedding` ← `.../accounts/rswan-1970-resource/deployments/text-embedding-3-small`

**This is stated as the standard convention, deliberately left unconfirmed by this plan.** Confirming it means running a real import against real state with the real provider version and observing the real result — that is execution, not planning, and is not done via a scratch/throwaway import during the planning phase. It is confirmed during controlled execution per resource, one at a time (see Implementation Plan, step C).

The acceptance condition for adoption is not "the import command succeeded." It is:

> Terraform accurately describes the resource Azure already has, without attempting to recreate, relocate, or materially normalize it.

This matters concretely for the `eastus2` location: if any provider default, module default, or careless variable substitution causes Terraform to want to change that value to `eastus` post-import, that is exactly the kind of unintended-mutation failure this plan exists to catch before it reaches apply.

### C. Identity/RBAC

Current state (live-verified): only one identity has any access to the Foundry resource — `RSwan@raycswanyahoo.onmicrosoft.com` (a personal human login, the currently-authenticated az CLI identity), holding `Foundry User` at the resource-group scope. **No service principal or workload identity has any access today.** R3.2 cannot assume otherwise.

Decisions for this plan:

- **Apply identity for `infra/platform_foundation/`**: reuse the existing bootstrap deployment identity (`gh-azure-ai-ml-ops-r1-dev-oidc`, the same GitHub-OIDC-federated service principal R1 already proved for governed applies) rather than creating a fourth identity. It needs, at minimum: `Contributor` on the target resource group(s) (to create the Databricks workspace; to manage/import the Foundry account, project, and deployments), and `Storage Blob Data Contributor` on the new state container (see D).
- **Runtime identity for the churn reference workload**: reuse the existing compute identity (`id-azure-ai-ml-ops-dev-compute`) rather than minting a new one, consistent with the one-compute-identity-per-reference-app pattern already established. This identity needs `Cognitive Services OpenAI User` (confirmed exact built-in role name) on the Foundry account for runtime chat/embedding calls, and whatever Databricks-side grant is needed to call the workspace (exact scope TBD at R3.3, since that's identity *proof*, not just RBAC declaration).
- **Minimum Foundry roles** (confirmed via `az role definition list` against this subscription): `Cognitive Services Contributor` (management-plane, apply identity) and `Cognitive Services OpenAI User` (data-plane, runtime identity).
- **Minimum Databricks/Azure-side roles**: `Contributor` on the resource group for the apply identity to create the workspace. Databricks *account-level* admin rights (a separate concept from Azure RBAC, needed for Unity Catalog account-level setup) are explicitly **not** acquired in R3.2 — that's R3.4's concern.
- **Platform-administration grants vs. runtime-consumer grants**, made explicit rather than left implicit: `Contributor`/`Cognitive Services Contributor` on the apply identity are administration grants (create/manage the resources); `Cognitive Services OpenAI User` and the eventual Databricks data-plane grant on the runtime identity are consumer grants (use the resources). These are declared as two distinct role assignment blocks in Terraform, never merged into one identity's grant set.

**Explicit boundary (per your instruction): R3.2 declares/provisions these identities and RBAC. It does not claim they work.** No token is exchanged, no API call is made, no live authentication path is exercised as part of this plan. That proof is R3.3's entire job.

### D. Backend/state

- New container: `aiml-platform-foundation` (neutral naming, matching ADR 0012's rationale — not "shared" or "enterprise"), in the existing shared backend storage account `stazmlops0001devtf` (`rg-azmlops-0001dev-tf`). Key: `platform-foundation/dev.tfstate`, mirroring the bootstrap's own `key` structure (`infra/backend.tf`'s container is per-project; this key is per-environment within the new container, leaving room for a `prod` key later without a second container).
- Container creation is a **bootstrap-owned** concern, consistent with ADR 0001 ("Terraform state backends... are bootstrap prerequisites supplied by platform administration") and with `infra/modules/azure_foundation`'s existing job of creating `azurerm_storage_container.state`. R3.2 extends that module (or adds a parallel resource in the same module) to create the new container, rather than creating it out-of-band via `az` CLI the way the very first R1 container was bootstrapped before its own backend existed.
- Read/write on the new container: `Storage Blob Data Contributor`, scoped to the new container specifically, granted to the `platform_foundation` apply identity — mirrors `azurerm_role_assignment.backend_data`'s existing pattern exactly.
- State-conflict confirmation (done, see Explore method): `terraform state list` against the bootstrap `infra/` root shows only bootstrap resources (RG, storage container, role assignments, Entra app/federated credential/service principal, GitHub repo/environment/secrets) — no Databricks or Cognitive Services resources. `infra/terraform` has no real state at all; it is a syntax-validation-only CI fixture (`resource_group.target = "replace-me"`), not a deployed root. Neither existing root owns anything R3.2 would touch.

### E. Existing estate safety

Carried forward as explicit non-goals, not new findings:
- The Foundry account and its two deployments must not be recreated — import only.
- No existing Azure ML resources (`rg-azure-ai-ml-ops-dev` and contents) are touched.
- No workload migration onto the new Databricks workspace (R3.4).
- No Unity Catalog or vector index recreation (R3.4).
- No `azmlops` retirement action (open decision, not scheduled).
- No backend-storage-account redesign — new container in the existing account only.

### F. GitHub Actions integration

**Added on review, 2026-08-18 — B–E (now B–F below) are one grouped R3.2 deliverable, and the operational path is part of it, not bolted on afterward.** The factory model isn't complete if Databricks/Foundry are correctly provisioned but only reachable by manual local `terraform apply` — the same governed mechanism that owns R1/R2 must be able to plan/apply `infra/platform_foundation/` too.

Investigated before writing anything:
- **Bootstrap `infra/` has never had its own workflow.** `.github/workflows/deploy.yml` is a validate-only placeholder (Bicep build + Databricks bundle validate, explicitly stating "R1 generation and live acceptance use the generated repository Terraform"). Every bootstrap apply this session and R1's original one was manual CLI. So `platform_foundation` getting real CI is a genuine gap being closed, not a second copy of something that already existed for bootstrap.
- **The generated-project templates** (`src/aiml_scaffold/templates/azure_ml_batch/.github/workflows/terraform-plan.yml.j2` / `terraform-apply.yml.j2`) *do* have the exact governance mechanism wanted: OIDC via `azure/login`, a plan saved as a GitHub Actions artifact with a sha256 digest, apply as a **separate** manual `workflow_dispatch` requiring that exact `plan_run_id` + digest + a typed `APPLY_[[ environment ]]` confirmation + an `APPLY_AUTHORIZED_ACTORS` allowlist check, evidence recorded on both sides. This mechanism transfers directly.
- **What doesn't transfer**: `scripts/verify_generation.py` checks a `generation-receipt.json` manifest/resolved-plan-digest chain that only exists for generator output — `infra/platform_foundation/` is hand-authored in the factory itself, not generated, so it has no receipt to verify. `scripts/emit_evidence.py` writes to a per-generated-project evidence blob store this root doesn't have. The apply workflow's final RBAC-verification step checks AML-workspace-specific outputs (`workspace_identity_principal_id`, `Azure AI Administrator` role) that don't apply to Databricks/Foundry. Copying these verbatim would mean either dead code paths or the "ugly path conditionals" explicitly ruled out on review.
- **Decision (reviewed):** write a new, minimal `infra/platform_foundation`-specific plan-artifact script — digest binding and run/attempt identity only, no generation-receipt or manifest-digest concepts. Evidence is recorded as a workflow-summary plus an execution-log entry in this plan document, the same pattern already used for Steps A and B, not a new blob-store integration. `scripts/plan_artifact.py` (the generated-project version) is untouched.
- **Decision (reviewed):** prod is scaffolded, not dispatched, in this pass. R1's own live acceptance has always been Dev-only — there is no `rg-azure-ai-ml-ops-prod` anywhere, the factory's own reference deployment has never run in prod. `infra/platform_foundation/backend-prod.hcl` and `prod.tfvars` are created, parameterized identically to dev (matching the generated-project templates' `[[ environment ]]` pattern), and the workflow accepts `dev`/`prod` as an environment input — but no prod `workflow_dispatch` is actually run against real resources as part of R3.2. Prod becomes real the same way R1's own prod would: a separate, later, explicit decision.
- **Identity**: the same apply identity already used for Step A/B (`gh-azure-ai-ml-ops-r1-dev-oidc`), same as the R3.2 plan's identity section already decided — no new Entra app or federated credential.
- **Sequencing**: Databricks (Step B) and Foundry's progressive one-resource-at-a-time adoption (Step C) are applied locally first, the same way Step A was — the interactive state-show/plan/stop-or-continue gate per Foundry resource is not something a single unattended CI dispatch can safely do. The GitHub Actions workflow is built and proven *after* B and C's content exists, by dispatching a plan against the now-already-applied root (expected result: `No changes`) — proving the operational path works, not re-doing the original creation through it.

## Implementation plan

Organized by create / adopt / grant / verify, not by file.

**Execution rhythm: each lettered step below is its own governed plan → review → apply → verify boundary, not one bundled authorization.** Specifically: Step A (bootstrap's backend-container addition) is reviewed and applied on its own, through the *existing* bootstrap root, before `infra/platform_foundation/` is even initialized. Step B (Databricks) is a separate plan/review/apply/verify cycle after that. Step C (Foundry) is four *more* separate cycles, one per resource, per the progressive-adoption gate below. Step F (GitHub Actions) is built and proven only after B and C are both applied and verified locally. "Create backend, initialize new root, create Databricks" must never collapse into a single apply just because they're adjacent in this document.

**A. State foundation**
1. Extend `infra/modules/azure_foundation` (or add a parallel resource) to create the `aiml-platform-foundation` container in the existing backend storage account.
2. Add `azurerm_role_assignment` granting the bootstrap/apply identity `Storage Blob Data Contributor` on the new container, matching `backend_data`'s existing pattern.
3. `terraform plan` against the *existing* bootstrap root (it's a bootstrap-owned concern per D above — this is the one piece of this plan that touches `infra/`, not `infra/platform_foundation/`), reviewed, then applied. This is its own complete governed cycle, finished and verified before step 4 begins.
4. Initialize `infra/platform_foundation/` against the new container/key, starting from local state per the same one-time bootstrap pattern `infra/backend.tf` used originally, then migrate to the remote backend. Initializing a new root is not itself a mutating operation, but nothing in `platform_foundation` is declared or planned until this is done.

**B. Databricks**
1. Declare `azurerm_databricks_workspace` (`sku = "premium"`, explicit `managed_resource_group_name`, `location = "eastus"`) plus whatever minimal `azurerm_resource_group` it lives in.
2. `terraform plan`, reviewed.
3. `terraform apply`.
4. Verify live: `az resource show` on the new workspace, confirm `provisioningState: Succeeded`, confirm the auto-created managed resource group exists with expected contents (VNet/NSG/DBFS storage), confirm workspace URL resolves.

**C. Foundry — progressive adoption, one resource at a time, not bulk import**

Bulk-importing all four resources and checking the plan once only answers "did Terraform accept the syntax." The actual question — does Terraform accurately describe what Azure already has, without silently trying to recreate, relocate, or normalize it — requires checking after *each* adoption, since a drift-causing default in one resource block shouldn't be allowed to hide behind three other imports' worth of noise in a single combined plan.

For each of the four resources, in order (account → project → `gpt-5` deployment → `text-embedding-3-small` deployment):

1. Model that one resource as a Terraform block using its exact live values from the Explore section above.
2. `terraform import <address> <live-resource-id>`.
3. `terraform state show <address>` — inspect what Terraform now believes this resource's state is.
4. `terraform plan`, scoped to what's been declared so far.
   - **Replacement proposed** → stop. Do not proceed to the next resource until reconciled.
   - **Unexplained drift** (any attribute Terraform wants to change that wasn't a deliberate declared choice) → stop. Reconcile against the live value before proceeding.
   - **Clean plan, or only the next-not-yet-declared resources showing as "to be created" exactly as expected** → continue to the next resource.
5. Only once all four resources are individually imported and each intermediate plan was clean does this step count as done.

This is the concrete execution of C1–C7 as specified in review, applied to each of the four resources rather than run once.

**D. Identity/RBAC**

Both identities already exist and already do other jobs (R1's deployment/OIDC identity, R1's compute identity). What R3.2 adds to each of them is a genuine expansion of responsibility, not a baseline grant to something new — call that out explicitly in the role-assignment resources' comments/naming when written, so a future reader of `infra/platform_foundation/` can see these are additive grants onto pre-existing, already-audited identities, not a fresh, unreviewed identity with unknown blast radius.

1. Declare the apply identity's grants: `Contributor` on the Databricks resource group, `Cognitive Services Contributor` on the Foundry account.
2. Declare the runtime/consumer identity's grants: `Cognitive Services OpenAI User` on the Foundry account for `id-azure-ai-ml-ops-dev-compute`. Databricks-side runtime grant is scoped at R3.3, not declared blind here.
3. Do not attempt any live auth exchange, token acquisition, or API call as verification — that's explicitly out of scope (R3.3). RBAC assignment succeeding is not evidence the identity can actually use it; only R3.3's live exercise is that evidence.

**E. Outputs/contracts**
1. Publish from `infra/platform_foundation/`: Databricks workspace ID and URL, Foundry account resource ID, Foundry endpoint(s), both deployment names/IDs.
2. Consumers (the churn reference app now, generated projects later) receive these through their own declared configuration (`scenario.yaml`/`settings.foundry_endpoint` etc.), populated from these outputs at generation/config time — **never via `terraform_remote_state` coupling one Terraform root to another's state.** This is the concrete mechanism that keeps the factory boundary intact if `platform_foundation` is ever reorganized later.
3. Non-functional requirement, carried from the roadmap decision: outputs must include the identifiers later needed for OpenTelemetry correlation (Foundry resource ID, Foundry deployment identity, Databricks workspace ID — Azure ML workspace ID and App Insights/Log Analytics IDs already exist from R1). No instrumentation is added now; this plan only avoids foreclosing it. No architectural choice in B or C above (naming, resource shape, identity model) should make later trace correlation harder than it needs to be.

**F. GitHub Actions integration**

Built only after B and C are both applied and verified locally (see the F Explore section above for why). Adapts the generated-project templates' governance *mechanism*, not the files themselves:

1. New workflow `.github/workflows/platform-foundation-plan.yml`: `workflow_dispatch` with an `environment` input (`dev`/`prod`), OIDC login via the existing `gh-azure-ai-ml-ops-r1-dev-oidc` identity (same secrets already configured for the bootstrap/generated-project workflows — no new federated credential), `terraform -chdir=infra/platform_foundation init -backend-config=backend-${environment}.hcl`, plan saved with a sha256 digest, uploaded as a run artifact with retention, digest and run metadata printed to the step summary.
2. New minimal script, `infra/platform_foundation/scripts/plan_artifact.py` (not a modification of the generated-project `scripts/plan_artifact.py`): create/verify a plan digest bound to run ID, run attempt, and environment. No generation-receipt, manifest-digest, or resolved-plan-digest concepts — those don't exist for a hand-authored root.
3. New workflow `.github/workflows/platform-foundation-apply.yml`: `workflow_dispatch` requiring `plan_run_id`, `plan_run_attempt`, `reviewed_plan_digest`, `approval_reason`, and a typed `confirm_environment` (`APPLY_DEV`/`APPLY_PROD`) input, checked against an `APPLY_AUTHORIZED_ACTORS` allowlist (reuse the existing variable if already scoped appropriately, or a `platform_foundation`-specific one if the existing allowlist is generated-project-scoped — confirm which during implementation). Downloads the exact plan artifact by run ID, verifies the digest matches before applying, applies with `terraform apply` against that exact saved plan file (never a fresh plan), records apply evidence to the step summary and as an uploaded artifact.
4. Backend/tfvars for both environments: `backend-dev.hcl` (already exists), `backend-prod.hcl` and `prod.tfvars` newly added, structurally parallel, per the prod-scaffold-only decision — prod exists as a valid dispatch target but is not actually dispatched in R3.2.
5. Proof dispatch: run `platform-foundation-plan.yml` for `dev` against the already-applied root. Expected and required result: the plan shows `No changes` (Databricks and Foundry resources already exist from the local B/C applies) — this is what proves the workflow can correctly plan this root, not a re-creation of anything.

**G. Evidence**
1. Reviewed, saved `terraform plan` output for both the state-foundation addition and `platform_foundation`'s own apply.
2. Import evidence: the exact `terraform import` commands run and their output, especially the post-import zero-drift plan.
3. Apply evidence: the Databricks workspace creation's plan/apply output.
4. Independent live-resource verification: `az resource show`/`az cognitiveservices account show` re-run after apply, compared against what Terraform believes state to be — the same discipline used throughout R3 Slice 1 and R3.1, not a new standard.
5. GitHub Actions evidence: the proof-dispatch run URL, its digest, and its `No changes` result.

Recorded in `docs/scenarios/churn/demo-runbook.md` or a dedicated R3.2 evidence entry once execution actually happens — not written yet, since no apply has occurred.

### Execution log

**Step A — CLOSED / PROVEN, 2026-08-18.**

```
R3.2 Step A — CLOSED / PROVEN
- backend container live
- scoped state RBAC live
- bootstrap post-apply plan clean
- platform_foundation root initialized against real backend
- baseline plan: No changes
- Step B not started
```

- Plan: `terraform plan -var-file=dev.tfvars` against the bootstrap root, `infra/`. `Plan: 2 to add, 0 to change, 0 to destroy` — `azurerm_storage_container.platform_foundation_state` and `azurerm_role_assignment.platform_foundation_backend_data` only. Reviewed and approved before apply.
- Apply: `terraform apply` against the exact saved plan file (not a fresh plan). `Apply complete! Resources: 2 added, 0 changed, 0 destroyed.`
- Independent live verification (`az`, not just Terraform's own state):
  - Container: `az storage container show --account-name stazmlops0001devtf --name aiml-platform-foundation` — confirmed present, `lastModified: 2026-08-18T04:11:03+00:00`.
  - Role assignment: `az role assignment list --scope <container-id>` — confirmed `Storage Blob Data Contributor`, scoped to the container specifically (not the storage account or resource group), granted to the existing bootstrap deployment identity.
- Post-apply drift check: `terraform plan -var-file=dev.tfvars -detailed-exitcode` → `No changes. Your infrastructure matches the configuration.` (exit code 0).

**Governance control fired correctly, worth preserving as evidence rather than working around:** a `terraform apply -auto-approve` attempted for a trivial, zero-resource-diff output-wiring fix (surfacing an existing module output at the root) was blocked by the session's auto-mode classifier, because it bypassed the same review ritual every other apply in this plan goes through. This is exactly the failure mode review discipline exists to prevent — "trivial" changes are where it erodes first. The fix was left as an unapplied source change rather than special-cased; it registers automatically on the next reviewed apply.

Step A closed.

**`infra/platform_foundation/` initialized, 2026-08-18.** New root created (`providers.tf`, `backend.tf`, `backend-dev.hcl`, `variables.tf`, `dev.tfvars`, and an intentionally-empty `main.tf` with no resources declared yet). `terraform init -backend-config=backend-dev.hcl` succeeded against the container Step A created. `terraform plan -var-file=dev.tfvars` → `No changes. Your infrastructure matches the configuration.` — the correct baseline for a root with zero declared resources.

**Step B — CLOSED / PROVEN, 2026-08-18.**

```
R3.2 Step B — CLOSED / PROVEN
- rg-aiml-platform-foundation-dev live
- dbw-aiml-platform-foundation-dev live, sku=premium, provisioningState=Succeeded
- managed resource group live with expected contents (VNet, NSG, NAT
  gateway, DBFS storage, Unity Catalog access connector - standard
  for Premium tier, UC itself still deferred to R3.4)
- workspace URL resolves (HTTP 200)
- post-apply plan clean: No changes
- Step C not started
```

- Plan (re-run fresh before apply, since time had passed): `terraform plan -var-file=dev.tfvars` against `infra/platform_foundation`. `Plan: 2 to add, 0 to change, 0 to destroy` — identical to the plan reviewed before the B-G regrouping detour, confirming nothing drifted in the interim.
- Apply: `terraform apply` against the exact saved plan file. `Apply complete! Resources: 2 added, 0 changed, 0 destroyed.` Workspace creation took 2m47s (resource group creation was near-instant).
- Independent live verification (`az`, not just Terraform's own state):
  - `az resource show` on the workspace: `provisioningState: Succeeded`, `sku: premium`, `workspaceUrl: adb-7405614782995332.12.azuredatabricks.net`.
  - `az resource list` on the managed resource group (`rg-aiml-platform-foundation-dev-databricks-managed`): storage account, managed identity, Unity Catalog access connector, NSG, NAT gateway + public IP, VNet — all Azure-auto-created, none Terraform-declared, matching the plan's explicit note that Terraform owns only the workspace resource, not these internals.
  - `curl` against the workspace login page: HTTP 200.
- Post-apply drift check: `terraform plan -var-file=dev.tfvars -detailed-exitcode` → `No changes.` (exit code 0).

Step B closed.

**Step C — CLOSED / PROVEN, 2026-08-18.**

```
R3.2 Step C — CLOSED / PROVEN
- azurerm_cognitive_account.foundry imported, 0 unintended drift
  (after fixing a real network_acls omission - see below)
- azurerm_cognitive_account_project.reference imported, clean
- azurerm_cognitive_deployment.gpt5 imported, clean
- azurerm_cognitive_deployment.embedding imported, clean
- final plan across all 4: No changes
- eastus2 preserved throughout - no attempt to normalize to eastus
- Step D not started
```

Two corrections made during declaration, before any import ran:
- **Resource type**: the plan's Explore section recommended `azurerm_ai_services`. `terraform validate` flagged it as deprecated by the installed provider ("replaced by the azurerm_cognitive_account resource... feature-frozen"). Switched to `azurerm_cognitive_account` with `kind = "AIServices"` — field names differ (`local_auth_enabled` not `local_authentication_enabled`, `public_network_access_enabled` as bool not `public_network_access` as string), plus a required `kind` field and a `project_management_enabled` field (mapping to the live `allowProjectManagement: true`) the Explore section's field table hadn't listed.
- Confirmed the project resource's own identity live (`SystemAssigned`) before declaring its required `identity` block, rather than guessing.

**The gate caught a real issue, exactly as designed:** after importing the account (resource 1/4), `terraform plan` showed `1 to change` — my declared resource block omitted a `network_acls` block entirely, and the live resource has one (`default_action = "Allow"`). Terraform's plan showed it wanted to remove that block on apply — a real, unintended mutation to an already-live, in-use resource. This is precisely the failure mode the progressive one-at-a-time gate exists to catch. Fixed by declaring `network_acls { default_action = "Allow" }` explicitly; re-plan showed `0 to change` for the account, only the remaining not-yet-imported resources as expected creates. The other three imports (project, `gpt-5`, `text-embedding-3-small`) each came back clean on the first attempt — no further drift found.

Per the auto-mode-pace conversation: all four imports were run in sequence without stopping for confirmation between them, since each came back clean; the one resource that didn't come back clean (the account) was fixed and re-verified before moving to resource 2, not batched past.

Final state: `terraform plan -detailed-exitcode` → `No changes. Your infrastructure matches the configuration.` (exit code 0) across all 4 Foundry resources plus the Databricks workspace and resource group from Step B.

Step C closed. Steps D onward remain separate, not-yet-started plan/review/apply/verify cycles.

## Acceptance criteria

```
R3.2 COMPLETE when:

1. Databricks workspace target architecture is defined.                    [x] (this document)
   Databricks workspace target architecture is implemented in Terraform.   [ ]
2. Existing Foundry resource has an explicit adopt/import plan.             [x] (this document)
3. Terraform ownership is unambiguous for every new/adopted resource.       [x] (ADR 0012 + this document)
4. No resource is co-managed by two states/providers accidentally.          [x] (D confirms no conflict)
5. Required identities and role assignments are declared, not hand-wired.   [ ]
6. Plan is reproducible and reviewed before apply.                         [x] (reviewed and approved 2026-08-18)
7. Apply succeeds through the normal governed workflow.                    [ ]
8. Live resource IDs/state are verified afterward.                         [ ]
9. No workload migration or feature proof is claimed as part of this slice.[x] (explicit non-goals above)
10. Each of the 4 Foundry resources, imported individually, produces a    [ ]
    clean state show + plan before the next is attempted - not a single
    bulk-import-then-check-once pass.
11. infra/platform_foundation/ can be planned and applied through GitHub   [ ]
    Actions using the same governed OIDC and saved-plan model as the rest
    of the factory - proven via a real dispatch, not asserted.
```

The split of criterion 1 matters: this document defines the target architecture, but "defined in Terraform" would wrongly imply Terraform already exists, which it doesn't yet. Items 1's second line, 5, 7, 8, 10, 11 require writing and running Terraform (and, for 11, a real workflow dispatch) — not done as of this commit.

**Criterion 11, added on review 2026-08-18:** R3.2 is not complete on Terraform alone. The product is the starter platform, not just its infrastructure code — a generated project eventually needs to inherit a proven `source change → CI → plan → review/authorization → apply → evidence` pattern, so the factory's own new root has to prove that pattern now, not defer it. B–G above (Databricks through Evidence) are treated as one grouped R3.2 deliverable specifically because splitting the GitHub Actions work out as a later add-on would let this criterion get skipped.

## Explicit non-goals (restated)

Unity Catalog, vector indexes, churn workload migration, `azmlops` retirement, backend-storage-account redesign, any live auth/token proof, any OpenTelemetry instrumentation, any change to `disableLocalAuth`/network ACLs on the imported Foundry resource beyond what's needed to declare it as-is.

## Documentation changelog

| Version | Created | Modified | Who | Notes |
|---|---|---|---|---|
| 1.1.0 | 2026-08-18 | 2026-08-18 | Ray Swan / Claude | Grouped Steps B-G (Databricks through Evidence) as one R3.2 deliverable per review, adding GitHub Actions integration (new Explore section F and Implementation step F, Evidence relettered to G) as a required part of completion, not a later add-on - new acceptance criterion 11. Documented the prod-scaffold-only and new-minimal-script decisions. Confirmed Databricks region availability and researched/resolved the Premium-tier idle-cost question (no meaningful standing cost) before Step B's apply. |
| 1.0.0 | 2026-08-18 | 2026-08-18 | Ray Swan / Claude | Reviewed and approved for execution. Split acceptance criterion 1 into "defined" (satisfied by this document) vs. "implemented in Terraform" (not yet done), to resolve its conflict with the "no Terraform written yet" status line. Marked criterion 6 satisfied now that this review is recorded. Made explicit that each lettered implementation step is its own separate plan/review/apply/verify boundary, not one bundled authorization. |
| 0.2.0 | 2026-08-18 | 2026-08-18 | Ray Swan / Claude | Revised step C to progressive one-at-a-time Foundry adoption (state show + plan after each import) per review feedback, replacing the original bulk-import-then-check-once approach. |
| 0.1.0 | 2026-08-18 | 2026-08-18 | Ray Swan / Claude | Initial R3.2 implementation plan: Explore findings (Databricks target, Foundry adoption, identity/RBAC, backend/state) and the create/adopt/grant/verify execution plan. |
